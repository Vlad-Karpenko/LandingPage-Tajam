# LandingPage-Tajam

## Clone the project:

git clone https://github.com/Vlad-Karpenko/LandingPage-Tajam.git

## Upload npm packages:

npm install

## Create build folder

gulp build

## Create build folder and run server:

gulp server
